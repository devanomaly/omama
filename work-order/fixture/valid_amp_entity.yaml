# Clean case: an HTML entity `&#39;` in a pattern is not a command boundary
goal: "whatever"
non_goals: ["nothing"]
tier: S1
task_type: implementation
done_when: ["something observable"]
verify: 'grep -q "&#39;" out.html'
