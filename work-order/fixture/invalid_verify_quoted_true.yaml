# Planted violation: vacuous verify spelled with quotes -- bash joins 'tr'"ue" into `true`
goal: "whatever"
non_goals: ["nothing"]
tier: S1
task_type: implementation
done_when: ["something observable"]
verify: 'pytest -q || ''tr''"ue"'
