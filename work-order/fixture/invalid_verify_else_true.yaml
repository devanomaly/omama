# Planted violation: vacuous verify behind the reserved word `else`
goal: "whatever"
non_goals: ["nothing"]
tier: S1
task_type: implementation
done_when: ["something observable"]
verify: "if pytest -q; then exit 0; else true; fi"
