# Clean case: a REAL command after `||`
goal: "whatever"
non_goals: ["nothing"]
tier: S1
task_type: implementation
done_when: ["something observable"]
verify: "test -f CHANGES.md || test -f CHANGELOG.md"
