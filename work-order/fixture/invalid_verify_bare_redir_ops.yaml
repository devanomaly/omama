# Planted violation: vacuous verify behind all seven bare redirection operators + target
goal: "whatever"
non_goals: ["nothing"]
tier: S1
task_type: implementation
done_when: ["something observable"]
verify: "pytest -q || > /dev/null >> /dev/null < /dev/null 2> /dev/null >& 2 <& 0 &> /dev/null true"
