# Planted invalid case #2 -- task_type: bugfix but reproduction.required: false.
observed_failure: "POST /checkout returns 500 when the cart has an item with an expired discount"
hypothesis: "discount validation code doesn't handle expired discounts"
task_type: bugfix

reproduction:
  required: false
  acceptable_evidence: [failing_test]

allowed:
  files:
    - src/checkout/discount_validator.py
  commands:
    - "pytest tests/checkout/test_discount_validator.py"

forbidden_effects:
  - "direct deploy to production"

acceptance_tests:
  - "pytest tests/checkout/test_discount_validator.py::test_expired_discount_rejected"

required_evidence_artifacts:
  - "output of the failing test before the fix"

budget:
  tokens: 200000
  wall_time: "30m"
  tool_calls: 40

rollback: "git revert of the fix commit"

completion_rule: "all acceptance_tests green"
