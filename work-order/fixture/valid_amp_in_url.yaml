# Clean case: `&` inside a quoted URL is inside a word, not a command boundary
goal: "whatever"
non_goals: ["nothing"]
tier: S1
task_type: implementation
done_when: ["something observable"]
verify: 'curl -fsS "http://localhost:8000/health?probe=1&deep=1"'
