# Planted violation: vacuous verify inside a `{ ...; }` group
goal: "whatever"
non_goals: ["nothing"]
tier: S1
task_type: implementation
done_when: ["something observable"]
verify: "pytest -q || { true; }"
