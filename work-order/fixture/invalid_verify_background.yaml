# Planted violation: backgrounded verify -- a trailing `&` discards the exit status
goal: "whatever"
non_goals: ["nothing"]
tier: S1
task_type: implementation
done_when: ["something observable"]
verify: "pytest -q &"
