# Planted violation: vacuous verify glued to its own redirection (`true>/dev/null`)
goal: "whatever"
non_goals: ["nothing"]
tier: S1
task_type: implementation
done_when: ["something observable"]
verify: "pytest -q || true>/dev/null"
