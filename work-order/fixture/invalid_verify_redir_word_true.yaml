# Planted violation: vacuous verify behind a glued redirection word (`>/dev/null`)
goal: "whatever"
non_goals: ["nothing"]
tier: S1
task_type: implementation
done_when: ["something observable"]
verify: "pytest -q || >/dev/null true"
