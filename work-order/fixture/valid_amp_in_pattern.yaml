# Clean case: `&` inside a quoted pattern is inside a word, not a command boundary
goal: "whatever"
non_goals: ["nothing"]
tier: S1
task_type: implementation
done_when: ["something observable"]
verify: 'grep -q "a&b" report.txt'
